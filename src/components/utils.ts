import { cva } from "class-variance-authority";

export const outlineStyle = cva("");
// export const outlineStyle = cva("outline-dashed outline-destructive");
export const sectionSpaceStyle = cva("my-8 py-8");
